name: sams_attendance
description: SAMS — Manage Student Attendance Module
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  shared_preferences: ^2.5.5
  geolocator: ^14.0.2
  permission_handler: ^12.0.3
  intl: ^0.19.0
  flutter_secure_storage: ^10.3.1
  # FIX: Added for SAMS-REQ-414 — Export/download attendance report
  share_plus: ^13.1.0
  path_provider: ^2.1.5
  printing: ^5.13.2
  pdf: ^3.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
